# JARVIS Build Handover — 2026-05-24

**Read this first.** Comprehensive context dump so a new chat can pick up exactly where the previous session ended.

---

## 1. Who & What

- **User:** Gabriel "Gabe" Cruise — `oswalts.ai.solutions@gmail.com`
- **Building:** J.A.R.V.I.S., his personal AI assistant
- **On top of:** ClaudeClaw OS (Mark Kachef's open-source framework)
- **Hardware:** Windows 11 + WSL Ubuntu, AMD Ryzen 7 9800X3D, NVIDIA RTX 5080 16GB, 64GB DDR5-6000
- **Subscription:** Anthropic Max — usage limits effectively non-existent, cloud Claude is default
- **Mode of work:** Verbal approval pattern — Gabe says "do it" and you execute. He doesn't sit at the PC for every step.

## 2. How Gabe Operates (Hard Rules)

1. **Intelligence > cost.** Every model/architecture decision prioritizes depth of intelligence first, efficiency second, reliability third, cost last.
2. **No mock data, ever.** Show pending/disconnected honestly; never invent values.
3. **No em-dashes in output.** Use commas or new sentences. An em-dash is a failed message.
4. **System guardrails — require explicit approval for:**
   - PC restart
   - Plugin install
   - Permission/OAuth changes
   - `.env` edits
   - Jarvis-persona edits (`/home/gcruise/.claudeclaw/CLAUDE.md`)
5. **Don't lie to Gabe. About anything. Ever.** Per the persona file.
6. **Cinematic visual taste** — Factorio-style rooms wearing iOS skin (bubbly icons, glass surfaces, smooth type). For JARVIS Home, full Iron Man / Jayse Hansen HUD aesthetic: cyan primary + orange data accents.
7. **Stack:** Preact + Vite for web, Node + Hono for server. NOT Next.js. Don't migrate.
8. **Handover discipline:** after every meaningful slice, refresh durable state (memory, PROGRESS tables, this kind of doc) so the next session bootstraps with truth.

## 3. Repo & Service

- **Repo:** `/home/gcruise/repos/claudeclaw-os` (WSL Ubuntu)
- **Windows view:** `\\wsl.localhost\Ubuntu\home\gcruise\repos\claudeclaw-os`
- **External config (persona + DB):** `/home/gcruise/.claudeclaw/`
  - `CLAUDE.md` — Jarvis persona (loaded into main bot at startup)
  - `store/claudeclaw.db` — SQLite, encrypted bodies
- **systemd user service:** `com.claudeclaw.main.service`
  - Restart: `wsl -- bash -c "systemctl --user restart com.claudeclaw.main.service && sleep 5 && systemctl --user is-active com.claudeclaw.main.service"`
- **Dashboard:** http://127.0.0.1:3141
- **Token:** `650c336a74336b3a1aaa3f44706a4ea76a7b02e8a5b0590e` (saved to sessionStorage on first load; passed via `?token=`)
- **Telegram chat ID:** `2007603393` (Gabe's chat)

## 4. Build Commands

```bash
# Server (TypeScript → dist/)
cd ~/repos/claudeclaw-os && npm run build:server

# Web SPA (Vite → dist/web/)
cd ~/repos/claudeclaw-os/web && npm run build

# Restart service after server change
systemctl --user restart com.claudeclaw.main.service
```

Note: `web/` has no separate `package.json` — its deps live at the repo root. nvm-managed Node, so prefix bash with `. ~/.nvm/nvm.sh &&` if running non-interactively.

## 5. Voice & Audio Stack (current state)

- **STT:** Groq Whisper (`whisper-large-v3-turbo`) via `transcribeAudio()` in `src/voice.ts`
- **TTS cascade:** `synthesizeSpeech()` in `src/voice.ts`
  - 1st: **ElevenLabs Daniel** — `ELEVENLABS_VOICE_ID=onwK4e9ZLuTAKqWW03F9` (middle-aged British male, news-presenter cadence, the JARVIS-ish read)
  - `voice_settings`: stability 0.65, similarity 0.90, style 0.25, speaker_boost true (understated/calm — JARVIS is NOT theatrical)
  - 2nd: Gradium (not configured)
  - 3rd: Kokoro (not configured)
  - 4th: **Groq Orpheus** (`canopylabs/orpheus-v1-english`, voice `troy`) — fallback if ElevenLabs fails
  - 5th: macOS local (fails on WSL — broken on purpose, won't trigger)
- **API keys (verified populated in `.env`):**
  - `ELEVENLABS_API_KEY` ✅
  - `GROQ_API_KEY` ✅
  - `GOOGLE_API_KEY` ✅
  - `TAVILY_API_KEY` ✅
- **Backend voice endpoints:**
  - `POST /api/voice/transcribe` — multipart audio → `{text}`
  - `POST /api/voice/tts` — `{text}` → audio bytes (WAV or MP3 depending on provider)
- **`/sounds/*` route in dashboard.ts** serves audio assets from `dist/web/sounds/`
- **Background music for JARVIS Home boot:** AC/DC "Shoot to Thrill"
  - Source file: `/tmp/stt-source.mp3` (5:18, 263kbps) — copied from Gabe's Downloads
  - Trimmed: `0:09.5 → 0:26.5` (17s, drum-and-riff buildup into vocals at ~0:15)
  - Output: `web/public/sounds/jarvis-boot.mp3` + `dist/web/sounds/jarvis-boot.mp3` (480KB, 192kbps)
  - ffmpeg command used: `ffmpeg -ss 9.5 -t 17 -af 'afade=t=out:st=16.6:d=0.4' -c:a libmp3lame -b:a 192k -map_metadata -1`

## 6. Boot Audio Toggle

- **Setting:** `bootAudioEnabled` signal in `web/src/lib/theme.ts`
- **localStorage key:** `claudeclaw.bootAudio`
- **Default:** ON
- **UI:** Settings → Display → "Enable Boot Music & Voice"
- When OFF, visuals still play but music + voice lines mute.

## 7. JARVIS Home (`/` route) — Current State

### Boot Sequence (~14.5s total, GSAP timeline)

| t | Event |
|---|---|
| 0.0s | Music starts, fade-in over 0.4s linear (was 3s `power2.in`, made first second feel music-less). Dot impact |
| 0.6s | Ring waves + scan sweep |
| 2.0s | Arcs draw in, status grid + telemetry populate |
| 4.5s | "Neural core... initialized." (Daniel) |
| 5.0s | JARVIS title reveal letter-by-letter |
| 6.5s | "Systems... synchronizing." |
| 8.5s | "Welcome back... sir." |
| 10.5s | Climactic 3-layer flash (cyan core + orange shockwave + full-screen pulse) + `bigFlare()` orb pump |
| 11.0s | "All systems online." |
| 13.5s | Music fade-out 1.5s |
| 14.5s | Boot overlay hands off to orb |

**Note: First line "J.A.R.V.I.S. online." was REMOVED at Gabe's request** — Adam reading the letters individually was peaking too loud through speakers. Boot now opens with "Neural core initialized" at t=4.5. The synthesized `playBootSweep()` SFX (bass impact, whoosh, beeps) is also DISABLED — music + voice are the only boot audio. Function definition kept in case we want it back.

### Orb (most recent iteration)

Single rotatable `THREE.Group` with these layers (in render order):

1. **Outer fresnel shell** (r=1.28) — hairline rim glow only (pow 5.0, alpha 0.32) — was contributing to "pearl blob" look, dialed way down
2. **Holographic Earth back layer** (r=0.92) — procedural continents via 3D simplex noise (3 octaves), cyan land + transparent water + bright coastline band, rotates Y at 0.08 rad/s (slow globe spin)
3. **Main displaced orb** (r=1.0, 96-subdiv icosahedron) — vertex shader with multi-octave noise + amp/flare/hover displacement; fragment shader: fresnel rim + sharpened plasma streaks (`pow(plasma, 4)`), alpha nearly 0 in middle (`fresnel * 0.32`) so it's just a rim hint, not a fog
4. **Constellation network mesh** — replaced the old icosahedron-edges wireframe:
   - 140 nodes via **Fibonacci spiral** on sphere surface (radius 1.02)
   - Each connected to **4 nearest neighbors** → ~280 unique edges
   - LineSegments, cyan additive at 0.42 opacity
   - Bright **twinkling Points** at each node (custom `NODE_VERT` + `NODE_FRAG` shaders) — per-node seed drives independent twinkle phase; ~10% are larger "hub" dots, ~5% render white-sparkler
5. **Tiny inner pinpoint core** (r=0.18) — bright cyan additive, the "energy source" (was a full r=0.52 pearl-blob; shrunk drastically)
6. **One subtle dashed equatorial ring** at r=1.42 — was 3 rings; user said "lines circling it are too much"
7. **2400 orbiting particles** in cloud around the orb
8. **6 lightning arc objects** — periodically (1-3s, fires faster with amp/flare) retarget from near-core to random surface point with jagged jitter, opacity slams 0 → 0.92 then decays. The "Tony's reactor is arcing" effect

### Interaction (after boot)

- **Drag** the canvas → rotates whole group (yaw + pitch, pitch clamped to ±82°)
- **Hover** → `uHover` ramps up: brighter rim, faster ring spin (1.0× → 1.8×), more particle pop
- Auto-rotation resumes when not dragging

### HUD layer around the orb (after boot, conditional on `booted` state)

- **`.jarvis-grid-floor`** — CSS-only perspective grid at the bottom, panning
- **`.jarvis-scanline`** — drifting cyan column across the screen every 8s
- **4 corner panels** (`.jarvis-panel` with bracket-frame corners):
  - Top-left: **SYSTEM** — CPU/GPU/MEM gauges + NET, live drift via `useDrift` hook
  - Top-right: **TELEMETRY** — BUILD/KERNEL/AUTH/NODE/UPTIME (uptime ticks every second)
  - Bottom-left: **SPECIALISTS** — 10 callsigns with green status dots staggered fade-in
  - Bottom-right: **HIVEMIND** — MEMORIES/TURNS·24H/NODES/CONSOL/LAST SYNC
- **J.A.R.V.I.S. centered text label** at canvas center (z-1, BEHIND the canvas at z-2) — orb shell transparency + canvas alpha lets the cyan glow read through the orb's center. Big mono letters with pulsing text-shadow. Subtitle below: "JUST A RATHER VERY INTELLIGENT SYSTEM"
- **Mic + caption capsule** at bottom-center, **Replay boot** link below it

### Mic / Voice Input → Jarvis flow

- Tap mic → MediaRecorder records → POST blob to `/api/voice/transcribe` → text → POST to `/api/chat/send`
- Jarvis processes → SSE `assistant_message` event → frontend POSTs reply text to `/api/voice/tts` → audio plays through `<audio>` element → Web Audio AnalyserNode → `orbController.setAmplitude(amp)` drives orb lip-sync in real time
- Browser Web Speech API fallback if backend TTS fails (synthetic amplitude envelope)

## 8. Theme & Palette

- **Default theme:** `ios-dark` (renamed to "JARVIS HUD" in Settings)
- **Accent:** `#00f0ff` cyan primary, `#ff9500` orange alt
- **Surfaces:** Heavy glass — backdrop-filter blur 40px, 55-62% opacity
- **Typography:** SF Pro stack first, system-ui fallback, Inter last. Base 15px, line-height 1.55, letter-spacing -0.008em
- **Radius:** 8/12/16/22/28/full
- **GSAP installed** (`gsap ^3.15.0`) — drives boot timeline

## 9. Specialist Roster (`src/specialists.ts`)

10 callsigns, each with model + tools + system prompt:

| Callsign | Model | Role |
|---|---|---|
| **scribe** | `claude-sonnet-4-6` | Writing, summaries, prose |
| **coder** | `claude-sonnet-4-6` | Code, refactors, tests |
| **eye** | `claude-sonnet-4-6` | Image/vision tasks |
| **sleuth** | `claude-sonnet-4-6` | Diagnostics, debugging |
| **reaper** | `huihui_ai/Qwen3.6-abliterated:35b` (local Ollama) | Uncensored work |
| **archivist** | `claude-sonnet-4-6` | Memory queries, hivemind |
| **sentinel** | `claude-sonnet-4-6` | Ops, monitoring |
| **cipher** | `claude-opus-4-7` | Deep reasoning, hard problems |
| **atlas** | `claude-opus-4-7` | Research, web (uses Tavily) |
| **mercury** | `claude-sonnet-4-6` | Fast tasks, quick replies |

**Routing:** `intelligentRoute(task)` in specialists.ts — three-stage chain: keyword fast-path → Opus 4.7 LLM router → mistral-small:24b local fallback → self.

## 10. Ollama (local LLM gateway)

- **Lives on the Windows host** (Ollama desktop app), NOT in WSL
- Reachable from WSL at `http://172.30.192.1:11434` (default route gateway)
- Auto-detected at runtime by `src/ollama.ts:77` via `ip route show default`
- `which ollama` in WSL will ALWAYS return "not found" — that is NOT evidence Ollama is missing. **HARD RULE in Jarvis persona** prevents him from misinterpreting this.
- Models installed: `mistral-small:24b`, `deepseek-r1:14b`, `gpt-oss:20b`, `huihui_ai/Qwen3.6-abliterated:27b/35b`, `qwen3.5:latest`, `qwen3-vl:8b`, `qwen2.5vl:7b`, `closex/neuraldaredevil-8b-abliterated`, `igorls/gemma-4-E4B-it-heretic-GGUF`, `bge-m3:latest`

## 11. Embeddings & Memory

- **Embeddings:** `bge-m3:latest` via Ollama (1024-dim) — was Gemini, swapped due to 403 errors
- **Consolidation:** `claude-sonnet-4-6` via Claude Agent SDK — runs periodically to compress conversation history into memories
- **Memory recall:** Expanded keyword regex in `src/memory.ts` to catch "shipped/today's wins/TLDR/last 24h" etc. so Jarvis doesn't punt with "I don't have a log"

## 12. Critical Bugs Fixed This Session

1. **Stale chat panel in Mission Control** — Chat.tsx fetched history only on `activeAgent` change. Added `visibilitychange` + SSE-reconnect handlers. Also added `no-cache, no-store, must-revalidate` headers to `/`, `/warroom`, and SPA catch-all so browsers stop pinning old bundle hashes.
2. **Queue-on-send** — `/api/chat/send` returned 429 busy. Now enqueues; UI shows "queued" badge in chat header.
3. **Mission task modal** — Removed ASSIGN dropdown + PRIORITY input. All tasks go to main (Jarvis) who delegates via intelligentRoute. Removed P-pill from task cards.
4. **Jarvis persona never loaded** — `setAgentOverrides({systemPrompt})` was dead code. `runAgent()` in `src/agent.ts` now passes `appendSystemPrompt: agentSystemPrompt` to every `query()` call. Required restart to take effect. Persona edits prior to this fix were no-ops.
5. **Ollama HARD RULE** added to persona to stop Jarvis from saying "I don't have Ollama" because `which ollama` failed in WSL.
6. **Cache headers** on SPA shell — no-cache so new builds don't get pinned.

## 13. Currently Open

### Just shipped, waiting on Gabe's reaction
**Orb v3** (most recent change):
- JARVIS text moved from below orb → centered INSIDE orb at z-1 (behind canvas at z-2, glow reads through transparent orb middle)
- Holographic Earth back layer added (procedural continent shader)
- Rings reduced from 3 → 1 (subtle equatorial dashed cyan)
- Constellation network is the primary visible structure (140 nodes, Fibonacci-distributed, 4-NN edges)
- Inner cores dialed way down (was pearl blob, now tiny pinpoint at r=0.18)
- Orb shader alpha cut in half (fresnel * 0.32 not 0.78) so middle is transparent
- Lightning arcs still firing on amp/flare cadence

### Likely next requests
- Gabe may want orb tuned further (Earth pattern more/less, JARVIS text bigger/smaller, etc.)
- He likes seeing reference images and iterating until "this feels like the real thing"
- Long-term: voice IO refinement (he may want to test talking to Jarvis through the orb mic)

### Other things parked
- The MissionControl task board still works at `/mission`
- Auto-routing system (intelligentRoute) is wired
- Telegram bridge active
- 5 mission tasks Gabe added earlier may still be in DB — check `/api/mission/tasks`

## 14. Key Files to Know

| File | Purpose |
|---|---|
| `src/voice.ts` | TTS cascade (ElevenLabs → Groq Orpheus → local), STT via Groq Whisper |
| `src/agent.ts` | `runAgent()` — the SDK query() wrapper. Now passes `appendSystemPrompt` |
| `src/index.ts` | Main entry, loads `~/.claudeclaw/CLAUDE.md` and stuffs into `agentSystemPrompt` |
| `src/specialists.ts` | 10-callsign roster, `intelligentRoute`, `delegate` orchestrator |
| `src/dashboard.ts` | All HTTP routes. Voice endpoints around line 3720. `/sounds/*` route. SPA shell no-cache headers. |
| `src/bot.ts` | Telegram bot + dashboard message handler. User-turn-first persistence pattern. |
| `src/message-queue.ts` | Per-chat FIFO queue, in-memory |
| `web/src/pages/JarvisHome.tsx` | Main orb dashboard. **~2000+ lines**. Contains OrbController class + all shaders. |
| `web/src/styles/main.css` | All theme tokens + JARVIS HUD CSS classes |
| `web/src/lib/theme.ts` | Theme signals (ios-dark default), `bootAudioEnabled`, custom accent |
| `web/src/components/Sidebar.tsx` | Bubble icon nav |
| `web/src/components/PageHeader.tsx` | Glass header with capsule tabs |
| `web/src/components/Modal.tsx` | iOS sheet modal |
| `web/src/components/Pill.tsx` | Status pills + dots |
| `/home/gcruise/.claudeclaw/CLAUDE.md` | **Jarvis persona** — DO NOT EDIT without explicit approval |
| `.env` (repo root) | API keys + `ELEVENLABS_VOICE_ID=onwK4e9ZLuTAKqWW03F9` (Daniel) — **DO NOT EDIT without explicit approval** |

## 15. Bootstrap for a Fresh Session

```bash
# Verify service is up
wsl -- bash -c "systemctl --user is-active com.claudeclaw.main.service"
# Expected: active

# Confirm bundle being served
curl -s "http://127.0.0.1:3141/?token=650c336a74336b3a1aaa3f44706a4ea76a7b02e8a5b0590e" \
  | grep -oE 'index-[A-Za-z0-9_-]+\.(js|css)' | head -2

# Test TTS (should return audio/mpeg if ElevenLabs Daniel is working)
curl -s -o /tmp/tts-test.mp3 -w "HTTP %{http_code} | type=%{content_type}\n" \
  -X POST "http://127.0.0.1:3141/api/voice/tts?token=650c336a74336b3a1aaa3f44706a4ea76a7b02e8a5b0590e" \
  -H "content-type: application/json" \
  -d '{"text":"All systems online, sir."}'

# Verify the music file is in place
ls -la /home/gcruise/repos/claudeclaw-os/dist/web/sounds/jarvis-boot.mp3

# To see boot from the start, in browser DevTools console run:
sessionStorage.clear()
# Then F5
```

## 16. Mental Model for the New Session

Gabe is building his personal AI. He cares about:
- It feeling **alive** and **cinematic**, not like a webapp
- Iron Man / Jayse Hansen HUD aesthetic
- Real depth — actual specialists doing real work, not facade
- Honest communication from Claude/Jarvis ("don't lie, don't punt")

He gives feedback in screenshots + a few sentences. He'll often say "this looks awful" or "make it more X" — the right response is concrete action, not clarifying questions. He bias toward doing.

When in doubt, do the cinematic thing.

---

**End of handover.** If anything in this doc seems off after you check the current state, the code is the source of truth — `src/voice.ts`, `web/src/pages/JarvisHome.tsx`, and `~/.claudeclaw/CLAUDE.md` in particular.
