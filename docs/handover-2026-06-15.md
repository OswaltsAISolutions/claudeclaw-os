# Handover, 2026-06-15 (session summary)

A long, productive session. Evergreen system context lives in `HANDOVER.md`; this is the point-in-time log of what changed today. For a fresh `resume agency` session, read `RESUME-agency-research.md` (its SESSION 4 block) first.

## 1. Good Nature pitch, the headline event
- **The Monday quarterly pitch happened and went WELL.** Gabe pitched COO Matt to become Good Nature's in-house AI person. Matt: very engaged, took notes throughout, only soft pushback ("usually master the technician role first"), is **escalating to CEO Alec**, and asked Gabe to send a **proposal package.**
- **Package built and in `C:\Users\GCruise\Desktop\Matt Quarterly\`:** `Good Nature - AI Proposal.html` (polished doc, visually verified), `Good Nature - AI Proposal (Deck).pptx` (12 slides, content-verified; NOT visually rendered, no LibreOffice on this box), plus the demo, leave-behind, cheat sheet, pitch v2, survey prep.
- Built by a **9-agent workflow** (research -> draft -> 3 adversarial reviews -> finalize). Verified accuracy items: Claude Certified Architect = Anthropic's first official cert, $100M Partner Network, 60q, pass=720/1000 (doc says only "passed"; NEVER cite 750 verbally, say "above 720"); dropped unverifiable "10,000 certified" + "4-of-6"; the 2 clients are COMMITTED not paying (B&B Roofing, We R One) and used only as illustrative examples; ZERO printed savings dollars by design.
- **Governing rule (all GN-facing work):** 100% accurate, zero BS, no overpromising. Pending Gabe before sending: confirm cert specifics + have the Anthropic credential-verification link ready. Phase-2 tech-stack mapping waits on Matt granting office access. Detail: memory `client_good_nature.md`.

## 2. Model swap, Fable 5 outage
- **Fable 5 went unavailable** ("Claude Fable 5 is currently unavailable"; live probe showed sleuth+prism hard-failing). Verified via the running service, not the chatbot UI alone.
- Moved all Fable-5 roles to **claude-opus-4-8**: main, router, sleuth, prism. Introduced a single source of truth: **`PRIMARY_MODEL`** (+ `PRIMARY_MODEL_DISPLAY`) in `src/config.ts`. To switch everything when Fable returns OR a better model ships: change those two constants + redeploy. Grep `FABLE5-TEMP` for the temporary spots. Each specialist now self-reports its model.
- Fixed an unrelated Google retirement: `gemini-2.0-flash` (404) -> `gemini-3.5-flash` in `src/gemini.ts` (verified live end-to-end). Detail: memory `project_fable5_upgrade.md`.

## 3. Agency lane
- Outreach tracking shipped (clients columns + `/api/clients/:id/touch` + Clients drawer panel).
- Per-dive **usage ledger** shipped (generic `usage_ledger` table + `src/usage-ledger.ts` + `docs/USAGE.md` + burn line on dive cards). DelegateResult now carries measured cloud tokens.
- The 5 hot-lead dives completed (Challengers / Clary / Euro / J.B. Express / Rainbow). Dive run remains PAUSED at ~43 ready / ~39 paused; resume after weekly reset + Gabe's go.

## 4. Cross-PC hive mind + repo hygiene
- Created `HANDOVER.md` (master onboarding), `HIVE-MIND.md` (cross-PC coordination protocol), `SESSION-REGISTRY.md` (live roster). The git repo (`OswaltsAISolutions/claudeclaw-os`, private) is the shared brain across Gabe's 3 PCs (main, desktop-2 "MultiPC AI System Architecture", gaming laptop). Layer 1 (async via git) is live; Layer 2 (live link via the networked service over Tailscale) is the desktop-2 lane's build.
- **Committed the live application source** (commit 1be8946, 57 files / ~15.6k lines): many core modules had never been in git, so a fresh clone now actually builds. Excluded secrets (added `.env.bak*` to `.gitignore`), the live DB, node_modules, video renders. Gate green before commit (tsc x2 + 1022 vitest).
- Stray file `E` at repo root = junk from a botched command; harmless, untracked, ignore or delete.

## 5. Gotchas surfaced today (added to HANDOVER §9)
- The old `agency-gate.sh` masked vitest failures (piped to tail, ate the exit code). Use an exit-code-aware gate.
- iOS Mail attachment preview blocks JavaScript, phone-facing HTML must be pure CSS (the GN demo auto-plays via CSS).
- HTML docs need an explicit `background:#fff` or they're unreadable in dark mode.
- No LibreOffice on the main PC, can't render .pptx -> images for visual QA here.

## 6. Pointers
- Evergreen system map: `HANDOVER.md`. Coordination board: `SESSIONS.md`. Live roster: `SESSION-REGISTRY.md`.
- Memories updated today: `client_good_nature.md`, `project_ai_agency.md`, `project_fable5_upgrade.md`.
